# seo-guildford.uk brain

Static Astro 5 + Tailwind 4 brochure site, ~18 pages, no pSEO (areas/services/industries are single hub pages, not per-suburb pages). GSC property `sc-domain:seo-guildford.uk`. Repo: gh:sunnyp81/seo-guildford, deploy via Cloudflare Pages git integration (push to master auto-builds, `npm run build`, output `dist`).

## 2026-07-07 recovery pass
- Diagnosis: young-domain ramp, not a demotion. 28d GSC: impressions 1363 to 3382 (up), clicks 1 to 2. Query-level avg positions are mostly 40 to 90+ across ~45 non-branded queries (seo agency guildford, seo surrey, technical seo guildford, etc) despite real page-1-quality content, so this reads as "still earning trust/authority", not lost rankings. GSC indexing check on 10 core URLs: only homepage indexed, the other 9 are "Discovered - currently not indexed" or "URL is unknown to Google" (services/, areas/, industries/, seo-audit, seo-pricing, free-seo-audit, ai-search-optimisation, about all not indexed). This is the real blocker: Google has not chosen to crawl/index most of the site yet.
- Fixed a hard-rule violation: swept 222 em/en dash characters across 18 .astro files (pages + Base.astro layout) with a byte-safe sed pass, replacing " — " with ", " and en dashes with hyphens. Also corrected resulting comma-separated `<title>` tags to use a colon instead (e.g. "SEO Guildford: Local Search Marketing for Surrey") for cleaner SERP titles. This was a compliance fix (no-em-dash rule), not a CTR rewrite pass.
- Fixed a fabricated-stats trust liability: homepage hero stats bar (`src/pages/index.astro`) claimed "3.2x Avg Traffic Growth", "97% Client Retention", "150+ Keywords Page 1", "8 yrs Search Experience" with zero backing data, on a domain that is itself unranked. Replaced with non-numeric service pillars (Technical / Local / AI Search / Free Audit) so nothing unverifiable is asserted.
- Case study pages (`case-studies/*.astro`) already carry an explicit "this is illustrative, anonymised" disclosure both on-page and in FAQ schema, so left as-is; the fictional 5-star Review schema on the property-firm case study is a residual soft risk (fake-review-schema pattern) not fixed this session, worth a look later.
- Did not touch service depth, schema, or FAQ content: pages already have LocalBusiness/Service/FAQPage/Breadcrumb schema, quick-answer definitional blocks, and 1500+ word depth per page. The bottleneck right now is indexation/trust, not content depth.
- Build verified clean (18 pages), pushed to GitHub for CF Pages auto-deploy. Submitted changed core URLs to Bing (see Bing Webmaster). No Google "request indexing" tool available via MCP; relies on CF Pages resubmitting sitemap and organic recrawl.
- Next step for Sunny: if pages are still "Discovered - not indexed" in 2 to 3 weeks, that points to a domain-trust/backlink issue rather than a content issue, consider building a handful of genuine local citations/backlinks (Surrey Chamber, local directories) mentioned in the services page copy, since the site has none yet.

## 2026-07-17 rebuild plan (foundation-first, then dominate)
- Confirmed the "10 days later" checkpoint: 9/10 core URLs are now "URL is unknown to Google" (worse than the prior "Discovered - not indexed" state — Google isn't recrawling). Query "seo guildford" real national position is 47-56 and worsening (539 impressions/1 click over 28 days); a VPN+incognito search showing pos 3 was geo-proximity personalization bias, not a real ranking — discard any pos-3 reading as evidence.
- Full multi-tier plan (Sonnet research → Opus-directed workstreams → Sonnet tactics → Fable 5 final synthesis) written to `REBUILD-PLAN.md` in this repo root. Two phases: **F1-F4 Foundation** (strip fake Review schema on both case studies + undisclosed ROI figures on the pricing page — live spam-policy risk; fix the dead `/tools/` index 404 + missing nav links; populate empty `sameAs: []` on all 10 schema blocks + get a real named person/phone/GBP; land ≥3 real local citations) gated by an exit metric of ≥9/10 core URLs indexed, THEN **D1-D5 Dominate** (activate existing conversion assets, AI/GEO citation optimization, blog/content freshness, backlink scaling, standing measurement loop).
- **F1 (Spam-Policy & Trust Remediation) done, Fable-reviewed, pushed.** Deleted the fake Review schema on both case studies (fictional "Director, Guildford Estate Agency" / "CTO, Surrey Research Park Startup" 5-star reviews). De-claimed SERP-facing metadata (schema `description` + `<Base>` title/description) on both case studies to drop bare stat claims (280% GBP calls, £2.4M sales, 340% growth, 45 leads/mo) — on-page body content left untouched, already covered by an existing visible "illustrative, anonymised" disclaimer under the H1. Added the same disclaimer pattern to the pricing page's ROI Examples section and, on Fable's review catching a gap the initial pass missed, to `case-studies/index.astro` above the card grid (the card teasers stated the same stats as fact with no nearby disclaimer) — also softened an FAQ line that claimed the figures "reflect genuine engagements", which contradicted the anonymised framing next to it. Grep-verified zero `Review`/`aggregateRating` schema in `src/` and `dist/`; build clean, 18 pages.
- Deliberately NOT done in F1: the "we/our team" language (about.astro, contact.astro, free-seo-audit-guildford.astro, seo-pricing-guildford.astro) and the "25,000 indexed pages" portfolio claim — both gated on F3's identity decision (named practitioner vs. solo-anonymous), which Sunny hasn't made yet. Fable's review agreed deferring this was correct rather than guessing at public-facing identity/brand-voice.
- Next action: F2 (indexation recovery — `/tools/` 404 fix, nav links, llms.txt/sitemap completeness, resubmit to GSC/Bing) is next in sequence, or make the F3 identity call first since it also unblocks the deferred F1 copy. Full detail in `REBUILD-PLAN.md`.

## 2026-07-18 F2 + F3-copy executed (Opus-led, Sonnet workers)
- **Identity decision made by Sunny: solo-anonymous, first-person-singular.** No named person, no Person schema. Site voice converted from fake-team "we/our" to honest solo "I/my".
- **F2 (indexation/crawl) done + deployed** (commit `eef498b`, CF Pages live-verified `/tools/` returns 200): built the missing `src/pages/tools/index.astro` (was a live 404 that all 3 tool pages' breadcrumb schema pointed at); added Tools + Pricing to desktop nav, mobile drawer, footer (`Base.astro`); cross-linked the 3 tools; completed `public/llms.txt` to full 18-URL coverage and stripped its 7 em dashes. Sitemap now lists all 18 URLs incl `/tools/`. Site is 19 pages (18 indexable + 404).
- **F3 copy done** (same commit): converted first-person-plural → singular across all 11 page bodies + matching FAQ schema (kept schema and visible copy in sync), removed all "our team"/"the team" claims (~9 instances) and the unverifiable "25,000 indexed pages" / "substantial portfolio" scale claims. Zero em/en dashes site-wide, zero Review schema (grep-confirmed).
- **Submitted for indexing:** GSC sitemap resubmitted (`sitemap-index.xml`, pending processing); Bing sitemap resubmitted + all 18 URLs pushed via Bing submit_url_batch (Bing site is verified). NOTE: Google "Request Indexing" is UI-only (not in the Search Console API), so per-URL Google indexing requests remain a manual Sunny action in the GSC URL Inspection tool.
- **Entity corroboration added (2026-07-18, commit after eef498b):** Sunny chose "use all my details but not name" + "company reg only". Added a footer legal line "SEO Guildford is a trading name of ND Media Ltd, registered in England and Wales, company number 10784524" and populated all 10 `sameAs` arrays with the Companies House record URL (`https://find-and-update.company-information.service.gov.uk/company/10784524`). Site stays name-free (no Person schema, no name in copy) but now has a real resolvable entity — the core F3 trust signal. Registered office (17 Gainsborough Rd, Reading RG30 3DB) added per Sunny's follow-up ("hide it somewhere small and inconspicuous") as a small muted footer line ONLY, deliberately kept OUT of LocalBusiness schema so structured data doesn't signal a Reading location and undercut the Guildford local positioning. Bank details (Mettle) never touched.
- **STILL BLOCKED ON SUNNY (cannot fabricate):** (1) **real phone number** — Sunny chose "add a phone" but hasn't supplied the digits yet; add to contact page + LocalBusiness schema once given; (2) Google Business Profile claim/verify (F4, biggest remaining trust lever); (3) LinkedIn / directory profiles to ADD to `sameAs` alongside the Companies House URL already there; (4) ≥3 real local citations (F4). These gate the rest of F4 and the D-phase "dominate" work.
- Next agent-doable step: nothing further in Phase 1 without Sunny's external accounts. Recheck indexation (`check_indexing_issues`) in ~2-3 weeks; if the 9 URLs move off "unknown to Google" the F2 architecture fix worked. Phase 2 (D1-D5) stays gated on the ≥9/10-indexed exit metric.
