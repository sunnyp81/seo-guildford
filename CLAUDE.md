# seo-guildford.uk brain

Static Astro 5 + Tailwind 4 brochure site, ~18 pages, no pSEO (areas/services/industries are single hub pages, not per-suburb pages). GSC property `sc-domain:seo-guildford.uk`. Repo: gh:sunnyp81/seo-guildford, deploy via Cloudflare Pages git integration (push to master auto-builds, `npm run build`, output `dist`).

## 2026-07-07 recovery pass
- Diagnosis: young-domain ramp, not a demotion. 28d GSC: impressions 1363 to 3382 (up), clicks 1 to 2. Query-level avg positions are mostly 40 to 90+ across ~45 non-branded queries (seo agency guildford, seo surrey, technical seo guildford, etc) despite real page-1-quality content, so this reads as "still earning trust/authority", not lost rankings. GSC indexing check on 10 core URLs: only homepage indexed, the other 9 are "Discovered - currently not indexed" or "URL is unknown to Google" (services/, areas/, industries/, seo-audit, seo-pricing, free-seo-audit, ai-search-optimisation, about all not indexed). This is the real blocker: Google has not chosen to crawl/index most of the site yet.
- Fixed a hard-rule violation: swept 222 em/en dash characters across 18 .astro files (pages + Base.astro layout) with a byte-safe sed pass, replacing " — " with ", " and en dashes with hyphens. Also corrected resulting comma-separated `<title>` tags to use a colon instead (e.g. "SEO Guildford: Local Search Marketing for Surrey") for cleaner SERP titles. This was a compliance fix (no-em-dash rule), not a CTR rewrite pass.
- Fixed a fabricated-stats trust liability: homepage hero stats bar (`src/pages/index.astro`) claimed "3.2x Avg Traffic Growth", "97% Client Retention", "150+ Keywords Page 1", "8 yrs Search Experience" with zero backing data, on a domain that is itself unranked. Replaced with non-numeric service pillars (Technical / Local / AI Search / Free Audit) so nothing unverifiable is asserted.
- Case study pages (`case-studies/*.astro`) already carry an explicit "this is illustrative, anonymised" disclosure both on-page and in FAQ schema, so left as-is; the fictional 5-star Review schema on the property-firm case study is a residual soft risk (fake-review-schema pattern) not fixed this session, worth a look later.
- Did not touch service depth, schema, or FAQ content: pages already have LocalBusiness/Service/FAQPage/Breadcrumb schema, quick-answer definitional blocks, and 1500+ word depth per page. The bottleneck right now is indexation/trust, not content depth.
- Build verified clean (18 pages), pushed to GitHub for CF Pages auto-deploy. Submitted changed core URLs to Bing (see Bing Webmaster). No Google "request indexing" tool available via MCP; relies on CF Pages resubmitting sitemap and organic recrawl.
- Next step for Sunny: if pages are still "Discovered - not indexed" in 2 to 3 weeks, that points to a domain-trust/backlink issue rather than a content issue, consider building a handful of genuine local citations/backlinks (Surrey Chamber, local directories) mentioned in the services page copy, since the site has none yet.

## 2026-07-17 rebuild plan (foundation-first, then dominate)
- Confirmed the "10 days later" checkpoint: 9/10 core URLs are now "URL is unknown to Google" (worse than the prior "Discovered - not indexed" state — Google isn't recrawling). Query "seo guildford" real national position is 47-56 and worsening (539 impressions/1 click over 28 days); a VPN+incognito search showing pos 3 was geo-proximity personalization bias, not a real ranking — discard any pos-3 reading as evidence.
- Full multi-tier plan (Sonnet research → Opus-directed workstreams → Sonnet tactics → Fable 5 final synthesis) written to `REBUILD-PLAN.md` in this repo root. Two phases: **F1-F4 Foundation** (strip fake Review schema on both case studies + undisclosed ROI figures on the pricing page — live spam-policy risk; fix the dead `/tools/` index 404 + missing nav links; populate empty `sameAs: []` on all 10 schema blocks + get a real named person/phone/GBP; land ≥3 real local citations) gated by an exit metric of ≥9/10 core URLs indexed, THEN **D1-D5 Dominate** (activate existing conversion assets, AI/GEO citation optimization, blog/content freshness, backlink scaling, standing measurement loop).
- **F1 (Spam-Policy & Trust Remediation) done, Fable-reviewed, pushed.** Deleted the fake Review schema on both case studies (fictional "Director, Guildford Estate Agency" / "CTO, Surrey Research Park Startup" 5-star reviews). De-claimed SERP-facing metadata (schema `description` + `<Base>` title/description) on both case studies to drop bare stat claims (280% GBP calls, £2.4M sales, 340% growth, 45 leads/mo) — on-page body content left untouched, already covered by an existing visible "illustrative, anonymised" disclaimer under the H1. Added the same disclaimer pattern to the pricing page's ROI Examples section and, on Fable's review catching a gap the initial pass missed, to `case-studies/index.astro` above the card grid (the card teasers stated the same stats as fact with no nearby disclaimer) — also softened an FAQ line that claimed the figures "reflect genuine engagements", which contradicted the anonymised framing next to it. Grep-verified zero `Review`/`aggregateRating` schema in `src/` and `dist/`; build clean, 18 pages.
- Deliberately NOT done in F1: the "we/our team" language (about.astro, contact.astro, free-seo-audit-guildford.astro, seo-pricing-guildford.astro) and the "25,000 indexed pages" portfolio claim — both gated on F3's identity decision (named practitioner vs. solo-anonymous), which Sunny hasn't made yet. Fable's review agreed deferring this was correct rather than guessing at public-facing identity/brand-voice.
- Next action: F2 (indexation recovery — `/tools/` 404 fix, nav links, llms.txt/sitemap completeness, resubmit to GSC/Bing) is next in sequence, or make the F3 identity call first since it also unblocks the deferred F1 copy. Full detail in `REBUILD-PLAN.md`.

## 2026-07-19 Full-site audit (5 dimensions) + fixes
Sunny: "Now audit all." Ran 5 parallel Sonnet auditors (technical-SEO/schema/links, accessibility, design-consistency/regressions, conversion/forms, content-voice/claims) + Opus objective checks. (The accessibility agent died on an API error mid-run - dimension only partially covered.) Fixed, built, pushed:
- **2 invisible-text bugs from redesign churn:** ROI calculator result box (`bg-brand` near-white -> `bg-surface-alt`+cyan border) and AI-search SVG diagram (white text on light boxes -> theme-consistent). Both were live broken-on-screen.
- **Broken form confirmation** on contact.astro + free-seo-audit.astro: relied on server-side `?sent` check (never runs on static build) -> added client-side confirmation script + banner. Primary lead forms now confirm.
- **Voice:** swept remaining "we/our/us" -> solo "I/my/me" across case studies, tool pages, seo-audit (schema + visible synced). Remaining "we" are legit (illustrative example quote + "how quickly can we start").
- **Dashes:** removed all `&mdash;` entities + spaced ` -- ` from visible copy (0 now).
- **FAQ schema<->visible drift:** synced schema `text` to visible `<details>` sitewide (~10 pages were drifted; Google FAQ policy). 
- **Pricing contradiction** fixed (home FAQ said £3,000, real top tier £3,500).
- **LocalBusiness address:** removed the Guildford locality PostalAddress (contradicted the disclosed Reading registered office); kept areaServed.
- Added `:focus-visible` a11y styling; redrew favicon dark/cyan (was navy/gold/serif leftover); trimmed 4 over-length meta descs (home/technical-seo/ai-search/services to ~150-155); contact message optional + honeypot hardened + "24 hours"->"one working day"; **added ContactForm to About** (the one landing page missing it).
- **STILL OPEN (medium/low, flag):** case-study Article schema missing `image`+`publisher.logo`; 3 service pages have `Offer` blocks with no numeric price (reconcile with pricing page £500/£750/£400); 3 tool titles slightly >60 chars; areaServed Hampshire inconsistent across schema blocks; tool-page breadcrumb `text-white/*` cosmetic (readable, not tokenised); dead CSS (`.pull-quote`/`.stats-bar`/`.rule`), unused `@fontsource/fraunces` dep, no-op `prose-brand`/`prose-lg`; a FULL accessibility pass (the auditor died) - contrast on all muted text, drawer aria, alt text.

## 2026-07-19 CRO: every landing page optimised for conversion
Sunny: "make each landing page, including home page optimised for conversion." Shipped a shared conversion layer + per-page CTAs (honest signals only, no fabricated testimonials/logos/counts on a young solo site):
- **Sticky mobile CTA bar** (Base.astro, fixed bottom, mobile-only, `md:hidden`): "Get your free SEO audit". Body gets bottom padding on mobile so it doesn't cover content.
- **Reassurance strip** (`.cf-reassure`) inside the shared ContactForm above the fields: reply within 1 working day / free, no obligation / direct specialist access / ND Media Ltd. So every landing page's form gets it.
- **Lower form friction:** message field now OPTIONAL (was required); added "or email hello@..." fallback + a free/no-obligation line.
- **Above-the-fold CTA on every landing page** (Sonnet workers): after the opening answer block, a `.cta-button` "Request a free audit" + `#enquire` "Ask a question" link + `.cta-note` microcopy. **Mid-page CTA band** on the long pages (technical-seo, seo-audit, ai-search-optimisation, industries). **Post-results CTA** on both case studies (highest-intent moment). Homepage hero got `.cta-note`.
- Conversion funnel is now: hero CTA -> top CTA -> mid CTA (long pages) -> bottom ContactForm (#enquire) -> sticky mobile bar always visible. All dark-product styled, content/schema/JS preserved, build clean.
- Known pre-existing (out of scope, flag): case-studies/index still uses "we/our" voice + FAQ schema ("what we guarantee") inconsistent with the solo-anonymous "I" voice used elsewhere (the F3 pass skipped case-studies). Worth a voice cleanup pass later.

## 2026-07-19 Redesign v3: DARK PRODUCT ("Linear at night") - complete overhaul
Sunny: "overhaul the design completely" -> picked "Dark product / Linear at night" from distinct options (print-editorial was "better but" not final). Full break, committed:
- Palette: near-black charcoal `#0A0B0E` canvas + elevated surface `#101218` + single electric-cyan accent `#22D3EE`. Restrained cyan glow on CTA/focus/card-hover only (NOT the dot-grid/gradient-hero AI cliche - hero uses a faint masked line-grid + one cyan radial).
- Type: Space Grotesk (display/UI) + Inter (body) + JetBrains Mono (data labels). Dropped Fraunces/Newsreader. Google-hosted.
- Components (global.css, all shared classes restyled -> whole site inherits): elevated hairline cards w/ cyan-glow hover, mono stat/spec numbers, cyan-bordered quick-answer, glow process nodes, cyan FAQ markers, dark glass header, cyan-accent footer rule.
- Dark forms: ContactForm inputClass + contact/free-audit inputs `bg-white`->`#14171E`. AI-search SVG diagrams + pricing Growth-tier border + ROI slider accents remapped from print emerald/cream/ink -> dark cyan palette (sed). Checker gauge track -> #23262E. OG card redrawn dark+cyan (Space Grotesk, `og.png.ts`).
- Verified: build clean 19 pages, 0 light-theme hexes left in src, cyan+charcoal tokens live in deployed CSS, all key pages + /og.png HTTP 200, no em dashes. NOT visually checked (screenshot tooling still blocked; sent Sunny the dark og.png). Design lineage this session: editorial-tech -> print-magazine -> DARK PRODUCT (current). If dark isn't it either, next best is a REFERENCE URL from Sunny (I'm designing blind).

## 2026-07-19 SEO/AEO pass: internal links, extractable answers/FAQs, OG images
Sunny asked "anything else from SEO to optimise for Google or ChatGPT." Framed honestly: on-page is now solid, the ceiling is off-site trust/indexation (GBP + citations + manual GSC index requests, all Sunny-only). Then shipped the agent-doable menu (3 Sonnet workers over disjoint file sets + Opus):
- **Internal linking** (crawl/indexation aid): contextual hub-and-spoke links across service pages, hubs, case studies, tools (each links to siblings + relevant case study + pricing + free audit), descriptive anchors, dedup'd against existing.
- **AEO** (ChatGPT/Perplexity): rewrote quick-answer blocks on core pages to be self-contained + pronoun-free + extractable; added ~14 factual FAQ Q&As from real sub-queries (WebSearch-sourced), FAQPage schema kept byte-synced with visible copy. Also corrected case-studies/index "200-400%" to the real "195% to 340%" range shown on-site.
- **OG images**: Satori endpoint `src/pages/og.png.ts` renders a 1200x630 print-style card (cream + Fraunces + oxblood rule + "seo-guildford.uk / Guildford, Surrey"); wired og:image + twitter:image + summary_large_image site-wide in Base.astro. Deps added: satori, @resvg/resvg-js, @fontsource/fraunces. Verified valid PNG produced at dist/og.png (48KB); NOT visually checked (sent the file to Sunny to eyeball). Currently ONE site-wide card, not per-page titles - per-page dynamic OG is an easy follow-up.
- **Fonts/CWV**: confirmed the redesign cut families 3->2 (variable fonts = single file each), display:swap + preconnect already set. No change needed; perf improved not regressed.

## 2026-07-18 Print-editorial redesign v2 + nav + StaticForms on every landing page
Sunny: "looks like claude design" (twice). Diagnosed that the tasteful ink+emerald+mono editorial-minimal look IS the current AI house style; refining it kept landing in the same place. Sunny picked "editorial / print-magazine" from distinct directions. Committed hard to a print identity (global.css full rewrite + homepage hero):
- **Type:** Fraunces (characterful serif display) + Newsreader (serif body + italic standfirst/pull-quotes). Dropped Bricolage + Figtree + JetBrains Mono entirely.
- **Palette:** warm newsprint cream `#F4EEE1` + warm-black ink `#26201A` + single oxblood/claret accent `#8A2B1E` (editorial red-pen). No emerald, no grotesque, no mono.
- **Idioms:** masthead double-rule, italic standfirst, drop cap (`.dropcap`) on article openers, hairline + `.rule-double`, small-caps kickers (`.eyebrow` restyled), `.pull-quote`, Roman-numeral contents row, catalog-style rule-topped `.service-card` (no boxes), square ink buttons, oxblood dash bullets. Squared corners throughout.
- Then Sunny: "main nav clashing" + "landing pages need a contact form to my StaticForms." **Nav:** trimmed desktop masthead from 8 links+CTA to 5 essentials (Services/Work/Pricing/Tools/About) + Free Audit CTA, single-tone `.masthead-word` wordmark + oxblood dot; moved Technical SEO / AI Search / Areas / Industries into the footer (mobile drawer keeps all). **Forms:** new `src/components/ContactForm.astro` posts to StaticForms (accessKey `sf_9e906eb6c00416b9d3354749`, hello@seo-guildford.uk) with a PER-PAGE subject line (source-attributable). Added to all 15 landing pages (home, 3 hubs, 4 service, 3 case studies, 4 tools); contact + free-audit keep their own forms. Confirmation banner shown CLIENT-SIDE on `?sent=1` (static site can't read query params server-side) - also fixes the invisible-confirmation bug Fable had flagged on the old forms.
- CAVEAT: screenshot tooling is BLOCKED in this environment (both Playwright MCP profile-lock AND claude-in-chrome script-injection timeout on every page, localhost + live). All design pushes this session are verified by build + grep + dist HTML inspection only, NOT visually. Sunny is the visual reviewer. Latent: contact.astro + free-seo-audit.astro still use the old server-side `?sent` check (broken confirmation) - fix them to match the component's client-side pattern next.

## 2026-07-18 Per-page de-AI design pass (Fable audit -> Opus + 5 Sonnet workers)
After the system redesign, ran a Workflow (`wf_df778234-3f5`): 18 Sonnet auditors (one per page) scored each page for "looks AI" tells, Fable synthesized the audit + fix spec. Core finding: the redesign shipped the palette but the editorial vocabulary (`.eyebrow`/`.mark`/spec strips) had only landed on index.astro, so 17 pages were "AI template, recolored." Executed the full fix (21 files):
- **global.css/Base.astro:** added missing `.prose h1` size rule (H1s were flattening to body size under Tailwind Preflight); deleted banned `.cta-glow` rule + the `.tilt-card` 3D-tilt JS listener in Base.astro.
- **Sitewide sweeps, grep-verified 0 remaining:** legacy gold `#D4AF37`, `cta-glow`, `tilt-card`, `rounded-xl`, `rounded-full`, `ring-2`, `text-red`.
- **Per page:** emerald single-accent everywhere (killed tools' gold sliders + the quiz's rainbow score-ramp -> emerald, tiers differ by label); hairline small radius; mono numbers/labels. Fixed TWO live contrast bugs (invisible white-on-cream CTA text on local-seo-checklist + seo-roi-calculator). Case-study stat grids -> shipped `.stat-value`/`.stat-label` components. Pricing "Most Popular" `rounded-full` pill -> inline `.eyebrow`, tier prices -> mono. Recolored the two AI-search SVG diagrams off gold/navy -> emerald/ink. services/ hub hero converted from dark tool-masthead to the light editorial `.hero`. CTA closers de-templated (dropped centered rounded boxes, added eyebrows). Numbered mono section eyebrows on industries + ai-search. Removed the redundant homepage capability strip (duplicated the hero spec; no real proof numbers to justify keeping it). Editorial vocabulary adoption: 1 file -> 14. All copy, schema, form endpoints, tool JS preserved. Build clean 19 pages, no em dashes. Pushed, CF redeploying.
- CAVEAT (same as before): could not screenshot (Playwright MCP profile lock, no local headless tool). Verified via build + grep + built-HTML spot-checks (services hero light, gold hex gone, contrast bugs fixed, pill gone). Still worth a live visual review to fine-tune spacing on the restructured heroes/CTAs.

## 2026-07-18 Redesign (editorial-technical system)
Sunny: "Design still looks like Claude made." Diagnosed the AI-template tells (dark navy->gold gradient hero + dot grid + animated growth-chart + gradient text + bounce scroll cue, glassmorphism lift-and-glow cards, navy+gold palette, `transition:all`). Rebuilt the design SYSTEM via `src/styles/global.css` (all shared classes restyled, so the whole site changed) + homepage hero markup + fonts link only, content/schema on all 18 pages untouched. New system (Linear/Vercel/Stripe reference): warm-paper canvas `#F6F4EE`, near-black ink `#15161B` display type, single **emerald accent `#0E6B4F`** replacing the template gold, **JetBrains Mono** added for eyebrows/labels/spec numbers (the "engineered" signal). Editorial asymmetric hero (mono eyebrow, big left-aligned headline with emerald underline `.mark`, ink CTA + `.cta-link`, hairline `.hero-spec` strip instead of the fake chart), flat hairline cards, mono FAQ markers, all `transition:all` replaced with specific props. `.page-hero` kept DARK ink (interior tool-page mastheads = deliberate light-home/dark-utility rhythm); `.gradient-text` restyled white->emerald for those dark bands only. Verified in compiled `dist`: emerald token processed, mono linked, new hero present, old chart gone, entity work intact. Commit pushed after the entity commits. NOTE: could not screenshot (Playwright MCP profile lock + no local headless tool); shipped on static verification since near-zero traffic + git-reversible. If revisiting, screenshot the live site to fine-tune spacing/contrast.

## 2026-07-18 F2 + F3-copy executed (Opus-led, Sonnet workers)
- **Identity decision made by Sunny: solo-anonymous, first-person-singular.** No named person, no Person schema. Site voice converted from fake-team "we/our" to honest solo "I/my".
- **F2 (indexation/crawl) done + deployed** (commit `eef498b`, CF Pages live-verified `/tools/` returns 200): built the missing `src/pages/tools/index.astro` (was a live 404 that all 3 tool pages' breadcrumb schema pointed at); added Tools + Pricing to desktop nav, mobile drawer, footer (`Base.astro`); cross-linked the 3 tools; completed `public/llms.txt` to full 18-URL coverage and stripped its 7 em dashes. Sitemap now lists all 18 URLs incl `/tools/`. Site is 19 pages (18 indexable + 404).
- **F3 copy done** (same commit): converted first-person-plural → singular across all 11 page bodies + matching FAQ schema (kept schema and visible copy in sync), removed all "our team"/"the team" claims (~9 instances) and the unverifiable "25,000 indexed pages" / "substantial portfolio" scale claims. Zero em/en dashes site-wide, zero Review schema (grep-confirmed).
- **Submitted for indexing:** GSC sitemap resubmitted (`sitemap-index.xml`, pending processing); Bing sitemap resubmitted + all 18 URLs pushed via Bing submit_url_batch (Bing site is verified). NOTE: Google "Request Indexing" is UI-only (not in the Search Console API), so per-URL Google indexing requests remain a manual Sunny action in the GSC URL Inspection tool.
- **Entity corroboration added (2026-07-18, commit after eef498b):** Sunny chose "use all my details but not name" + "company reg only". Added a footer legal line "SEO Guildford is a trading name of ND Media Ltd, registered in England and Wales, company number 10784524" and populated all 10 `sameAs` arrays with the Companies House record URL (`https://find-and-update.company-information.service.gov.uk/company/10784524`). Site stays name-free (no Person schema, no name in copy) but now has a real resolvable entity — the core F3 trust signal. Registered office (17 Gainsborough Rd, Reading RG30 3DB) added per Sunny's follow-up ("hide it somewhere small and inconspicuous") as a small muted footer line ONLY, deliberately kept OUT of LocalBusiness schema so structured data doesn't signal a Reading location and undercut the Guildford local positioning. Bank details (Mettle) never touched.
- **STILL BLOCKED ON SUNNY (cannot fabricate):** (1) **real phone number** — Sunny chose "add a phone" but hasn't supplied the digits yet; add to contact page + LocalBusiness schema once given; (2) Google Business Profile claim/verify (F4, biggest remaining trust lever); (3) LinkedIn / directory profiles to ADD to `sameAs` alongside the Companies House URL already there; (4) ≥3 real local citations (F4). These gate the rest of F4 and the D-phase "dominate" work.
- Next agent-doable step: nothing further in Phase 1 without Sunny's external accounts. Recheck indexation (`check_indexing_issues`) in ~2-3 weeks; if the 9 URLs move off "unknown to Google" the F2 architecture fix worked. Phase 2 (D1-D5) stays gated on the ≥9/10-indexed exit metric.
